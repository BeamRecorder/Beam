# Windows native engines

The verified development cache uses `x64/capture-engine-<version>.exe` and `arm64/capture-engine-<version>.exe`. See the [native-engine cache contract](../README.md); use only `bun run electron:dev`.
